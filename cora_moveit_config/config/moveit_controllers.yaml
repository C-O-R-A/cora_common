# TODO: #3 GENERATED ARTIFACT — this file is hand-written today but must be
# emitted by the configurator from robot_layout.yaml. The generator's version mis-nests `arm_controller` at top level instead of
# under `moveit_simple_controller_manager` — compare the (correct) nesting here.
# Contract: C-O-R-A/configurator#2. Generator: C-O-R-A/configurator#7.

# MoveIt uses this configuration for controller management

moveit_controller_manager: moveit_simple_controller_manager/MoveItSimpleControllerManager

moveit_simple_controller_manager:
  controller_names:
    - arm_controller
    - gripper_fingers_controller

  arm_controller:
    type: FollowJointTrajectory
    joints:
      - J1
      - J2
      - J3
      - J4
      - J5
      - J6
    action_ns: follow_joint_trajectory

  gripper_fingers_controller:
    type: ParallelGripperCommand
    action_ns: gripper_cmd
    default: true
    joints:
      - Finger1